apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-service
  annotations:
    metallb.universe.tf/allow-shared-ip: cava
spec:
  ports:
  - port: 5672
  - port: 15672
  selector:
    app: rabbitmq
  type: LoadBalancer
